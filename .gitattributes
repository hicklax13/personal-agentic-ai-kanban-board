# Store text files with LF line endings in the repository. Git converts them back to each
# platform's native line endings on checkout, so Windows and macOS users both get what they
# expect.
* text=auto
